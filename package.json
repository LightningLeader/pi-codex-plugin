{
  "name": "@agents365/pi-plugin-cc",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Use the Pi coding agent from Claude Code to review code or delegate tasks.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "files": [
    "plugins/",
    "scripts/",
    "package.json",
    "README.md",
    "README_CN.md",
    "LICENSE",
    "NOTICE"
  ],
  "scripts": {
    "bump-version": "node scripts/bump-version.mjs",
    "check-version": "node scripts/bump-version.mjs --check",
    "test": "node --test tests/*.test.mjs"
  }
}
