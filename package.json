{
  "name": "pi-codex-plugin",
  "version": "0.8.0",
  "private": true,
  "type": "module",
  "description": "Use the Pi coding agent from Codex to review code and delegate tasks.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "files": [
    "plugins/",
    "scripts/",
    "package.json",
    "README.md",
    "README_EN.md",
    "LICENSE",
    "NOTICE"
  ],
  "scripts": {
    "bump-version": "node scripts/bump-version.mjs",
    "check-version": "node scripts/bump-version.mjs --check",
    "test": "node --test tests/*.test.mjs"
  }
}
