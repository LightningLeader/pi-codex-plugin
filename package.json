{
  "name": "pi-codex-plugin",
  "version": "0.8.0",
  "private": true,
  "type": "module",
  "description": "Delegate investigation and implementation tasks from Codex to the Pi coding agent.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "files": [
    "plugins/",
    "scripts/",
    "package.json",
    "README.md",
    "README_EN.md",
    "LICENSE",
    "NOTICE"
  ],
  "scripts": {
    "bump-version": "node scripts/bump-version.mjs",
    "check-version": "node scripts/bump-version.mjs --check",
    "test": "node --test tests/*.test.mjs"
  }
}
